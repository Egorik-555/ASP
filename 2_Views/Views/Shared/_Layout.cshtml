<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
</head>
<body>

    @Html.ActionLink("Home", "Home") | @Html.ActionLink("Products", "Products") | @Html.ActionLink("Services", "Services") | @Html.ActionLink("About", "About")

    @RenderBody()

    <p> footer Heorhhhh</p>

    @RenderSection("scripts", required: false)
</body>
</html>
